spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8180/realms/hacnation
  cloud:
    gateway:
      enabled: false
    config:
      enabled: false
    discovery:
      enabled: false
  kafka:
    enabled: false
eureka:
  client:
    enabled: false
